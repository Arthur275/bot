# 工作总记录

## 2026-04-27

### 执行边界与恢复链第三轮收口（2026-04-27）
- 今天没有再扩机器人功能，而是继续对 `orchestrator / position_manager / exchange_adapter / state_store` 这条执行链和恢复链做第三轮横向复审，重点看 real-mode 安全边界、invalid runtime snapshot、recent fills 与 reconciliation 的相互作用。
- 收口 `src/bot/exchange_adapter.py` 的 live snapshot 安全门：entry / exit / protective stop 等真实命令现在都要求 runtime snapshot 必须有效，不能再把 `snapshot_valid=False` 的占位快照当成可执行依据；同时把 signer/config 失败与 transport 失败分开，前者继续显式报 `request_signing_failed`，后者才退化为 invalid snapshot。
- 清掉 `src/bot/state_store.py` 的重复 observed-position merge，并补齐对应回归，避免恢复态状态二次写回漂移。
- 把 `risk_assist_cycle` 的 audit payload 与返回 report 对齐，补进 `runtime_overview`，让 expected/runtime/observed 三层仓位语义能直接在 `audit.jsonl` 里并排复核。
- 这轮还找到了一个真正的计划死路径：`PositionManager` 一直消费 `recent_fill_sync_required`，但上游原本没有稳定生产这个标记；第一版修复把触发条件放得太宽，导致普通 recovery 场景被错误插入 `sync_recent_fills`。
- 最终把 `recent_fill_sync_required` 收窄为“runtime snapshot 有效、live position 已是 `ENTERED`、但 runtime size 和落盘 observed size 都仍为 0、且当前确实 out-of-sync”的补数恢复场景，不再污染普通 `protective_stop_missing`、invalid snapshot 或一般 recovery 流程。
- 这一轮的意义，不是再加一个辅助命令，而是把 `recent fills` 固定为“补齐已开仓但 size 仍未收敛”的窄恢复工具，避免执行层重新长出第二套策略脑。

### 本轮验证
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py -q` → `31 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_position_manager.py D:/eth_trading_bot/tests/test_exchange_adapter.py D:/eth_trading_bot/tests/test_state_store.py -q` → `77 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py D:/eth_trading_bot/tests/test_exchange_adapter.py D:/eth_trading_bot/tests/test_state_store.py D:/eth_trading_bot/tests/test_position_manager.py -q` → `108 passed`

### 当前状态
- 当前已知执行边界 / 恢复链问题基本收口，手头没有新的明确未修失败项；但按这轮复审结论，还不适合宣称“0 bug”，后续更像继续盯 1~3 个潜在 P1/P2 级隐藏边界。
- 下一轮应优先继续做横向复扫，不急着扩结构重构或新增执行功能。

### 记忆回读口径补记（2026-04-27）
- 按本次记忆回读结果，把机器人项目当前最关键的配置边界再明确记一遍：`D:\eth_trading_bot` 的 Binance 数据 key 与交易执行 key 不能混用，默认交易入口已经收口为 `BINANCE_TRADE_API_KEY / BINANCE_TRADE_API_SECRET`，不再把 `BINANCE_API_*` 当成真实下单入口。
- 这一条需要单独保留在机器人总记录里的原因，不是代码表面改了环境变量名，而是前面已经验证过：账户读取可以成功、但 `/fapi/v1/order/test` 持续报 `-2015` 时，根因完全可能是“机器人进程实际读到的是数据 key”，而不是 quantity、权限语义、策略动作或下单合同本身有问题。
- 因此后续只要继续推进 real / preflight / 权限排查，默认先检查两件事：一是机器人当前读到的是否真是 `BINANCE_TRADE_API_*`；二是当前进程是否为新启动、已经实际加载到 Windows 用户级环境变量。不要再把旧会话没刷新环境导致的缺 key/错 key，误判成交易所权限或执行语义问题。
- 这条补记同时也作为后续联调停点说明：如果新会话还未开启，就不继续在旧进程里反复测交易权限；先重开 Claude 会话或新终端，让用户级环境变量生效，再复测 `/fapi/v2/account` 与 `/fapi/v1/order/test`。

## 2026-04-27

### 交易 key 切换与权限排查补充
- 本轮继续沿 real-mode 联调往下收口，重点不是再改策略语义，而是把 Binance 交易权限与本地配置入口彻底排干净。
- 先确认了账户私有读取与下单权限的边界：`/fapi/v2/account` 可正常返回余额与 `canTrade=true`，但 `/fapi/v1/order/test` 持续返回 `401 / -2015`，因此问题不在数量合同、symbol、最小下单量或时间戳主链，而在“当前机器人实际读到的 key 与交易 key 不一致”这层。
- 期间顺手修了一处 signer 边界：`src/bot/binance_transport.py` 不再把 `PreparedAdapterRequest.body` 里的本地审计字段一起并入真实签名参数，避免 `resolved_quantity / resolution_mode` 这类本地摘要字段误发给 Binance；同时补了 transport + adapter 回归，确认签名入参已经只保留真实请求参数。
- 继续补强 `preflight_commands(...)`：给 `entry_order` 的只读 preflight 加上与真实执行一致的 `-1021`（timestamp ahead of server time）刷新 server time offset 后重签处理，避免 preflight 先因为时间戳边界假失败。
- 之后通过脱敏指纹确认，机器人最初读到的仍是 `BINANCE_API_KEY / BINANCE_API_SECRET` 这组数据 key，而不是你实盘要用的交易 key；这也是前面为什么读账户可以、测试下单却始终 `-2015` 的直接根因之一。
- 已把 `src/bot/config.py` 的默认入口正式切到交易专用环境变量：`BINANCE_TRADE_API_KEY / BINANCE_TRADE_API_SECRET`，并补了 `tests/test_config.py` 锁定默认值，避免后续会话又退回读数据 key。
- 同时按“不要再靠当前临时 shell export”的口径，把 `BINANCE_TRADE_API_KEY / BINANCE_TRADE_API_SECRET` 写入 Windows 用户级环境变量。注册表层面已确认两项值都存在，且前缀分别为 `x7Cj6x...2uQJlX`、`JmNZwX...wvRbCk`；不在工作记录里保留完整明文。
- 当前停点也已明确：代码默认入口和用户级环境都已切到交易 key，但**当前这个 Claude 会话的工具子进程还没有刷新到新用户环境**，所以本会话内继续测会报 `Missing required environment variable: BINANCE_TRADE_API_KEY`。下一步不是再改逻辑，而是重开 Claude 会话/新终端，让新进程实际加载 `BINANCE_TRADE_API_*` 后再复测 `/fapi/v2/account` 与 `/fapi/v1/order/test`。

### 本轮补充验证
- `python -m pytest D:/eth_trading_bot/tests/test_binance_transport.py D:/eth_trading_bot/tests/test_exchange_adapter.py -q` → `40 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_config.py D:/eth_trading_bot/tests/test_binance_transport.py D:/eth_trading_bot/tests/test_exchange_adapter.py -q` → `43 passed`

### 当前附加状态
- 机器人默认交易入口现在已明确与数据入口分离：`BINANCE_API_*` 继续保留给数据侧，`BINANCE_TRADE_API_*` 留给真实交易执行。
- Windows 用户级环境已落交易 key，但旧会话不会自动回灌；后续任何“为什么还读不到交易 key”的排查都先看会话是否为新启动进程。

## 2026-04-27

### 今日新增进展
- 继续沿当前收口方向推进，没有扩新模块，改动集中在 `orchestrator` 审计载荷与对应测试。
- 给 `src/bot/orchestrator.py` 的 `shadow_cycle` / `risk_assist_cycle` 审计事件补入稳定摘要字段：`execution_result_summary` 与 `recent_fill_summary`，这样 review audit.jsonl 时不必每次展开完整 `execution_results` 才能判断是否有失败、失败是否属于 primary / auxiliary、以及最近成交摘要是否已经落盘。
- 补强 `tests/test_shadow_orchestrator.py`：除了保留 simulated-real 下 state_store 的摘要落盘断言，还新增直接读取 `audit.jsonl` 的断言，确认 `shadow_cycle` 事件中已经带上 `execution_result_summary` 与 `recent_fill_summary`。
- 继续补强 `tests/test_shadow_orchestrator.py`：新增 `risk_assist_cycle` 直接读取 `audit.jsonl` 的断言，确认风险循环也会稳定写入 `execution_result_summary` 与 `recent_fill_summary`，不只覆盖主 shadow cycle。
- 补强 `tests/test_exchange_adapter.py`：把 `advance_breakeven_stop` 与 `advance_trailing_stop` 在 `RuntimeMode.REAL` 下的阻断拆成显式测试，确认两类 strategy-relative stop 语义都会稳定返回 `unsafe_request_mapping`，且不会向 Binance transport 发出真实请求。
- 继续补强 `tests/test_shadow_orchestrator.py`：新增 `risk_assist_cycle` 的失败分类审计断言，明确锁定 `reduce_order` 失败会计入 `primary_failed`、`maintain_protective_stop` 失败会计入 `auxiliary_failed / protective_stop_failed`，避免后续只知道“有失败”却看不出失败层级。
- 继续补强 `tests/test_shadow_orchestrator.py`：新增 `risk_assist_cycle` 在 primary reduce 失败与 protective-stop 失败两类场景下的 state 回写断言，明确 `reduce_pending` / `reconciling`、`pending_action`、`recovery_required`、`reconciliation_required`、`protective_stop_required` 的真实恢复语义。
- 再补一条 simulated-real 风险循环回归：确认 `risk_assist_cycle` 不只会把 `recent_fill_summary` 写进 audit，也会把 `sync_recent_fills` 与 `reconcile_position_and_orders` 的结构化摘要真正回写到 `state_store`，包括最新 `recent_fill_summary` 与 reconciliation 后的 `observed_position_size_pct`。
- 继续补强 `tests/test_shadow_orchestrator.py`：新增 reconciliation `reason_codes` 与本地恢复状态的组合断言，确认 `position_size_mismatch + protective_stop_missing` 这类 runtime gap 会和 `reconciling / recovery_required / protective_stop_required` 保持一致，而不会只在 report 层短暂可见。
- 再补强 `risk_assist_cycle` 的 audit payload 组合断言：当 `reduce_order` 与 `maintain_protective_stop` 同时失败时，审计里不仅要保留 `execution_result_summary` 与 `reconciliation.reason_codes`，还要明确钉住 `state.execution_state=reduce_pending`、`state.pending_action=""`、`recovery_required/reconciliation_required/protective_stop_required=true` 这组真实优先级语义，避免后续误以为主执行失败一定会保留 pending_action。
- 按“机器人直接用量化、不再保留独立逻辑”的新口径收口 `src/bot/position_manager.py`：保留量化 `handoff.action` 作为主动作来源，机器人只负责 guard 裁决与附加执行项（protective stop / reconciliation / recent fills / breakeven / trailing）。
- 去掉 `observe_only / recovery_reconciliation_required / risk_management_only` 这类机器人二次主动作语义，把非 guard 阻断场景统一收口为量化动作直通；新增 `quant_action_passthrough` 作为执行层原因，明确这不是机器人自己再做一次策略判断。
- 继续把允许执行场景里的 `plan_reason` 全面收口到 `quant_action_passthrough`：不再区分 `entry_allowed / reduce_allowed / exit_allowed` 这类旧机器人语义，避免 audit/state/report 里继续残留“第二层策略脑”的痕迹。
- 同步更新 `tests/test_position_manager.py`、`tests/test_shadow_orchestrator.py`、`tests/test_state_store.py`、`tests/test_exchange_adapter.py`：把允许执行与恢复卫生动作场景的断言统一改成“量化动作直通 + 执行层原因”口径，并确认 command reason / state.last_plan_reason / audit.payload.plan_reason 已整体跟上。
- 继续标准化 `command reason`：把 primary command 从沿用 `plan_reason` 改成显式 `effective_action:<action>`，让 order/reduce/exit 这层在 audit、adapter action、execution result 里更容易直接对账，不再和 guard/runtime 级的 `plan_reason` 混在一起。
- 同步补强 `tests/test_exchange_adapter.py`：新增对 `entry_order / reduce_order` 及辅助命令 `reason` 的显式断言，确认主动作、protective stop、breakeven、trailing、recent fills、reconciliation 六类命令现在各自有稳定可识别的原因标签。
- 继续补强 `src/bot/orchestrator.py` 的展示层：给 `ShadowCycleReport` / `RiskAssistCycleReport` 新增 `command_reasons`，并把同一字段写入 `shadow_cycle` / `risk_assist_cycle` audit payload，方便后续直接并排查看 `requested_action / effective_action / command_reasons` 三层语义是否一致。
- 继续补强 `src/bot/orchestrator.py` 的展示层：在已有 `command_reasons` 基础上，再新增紧凑 `command_summary`（`target / reason / operation`），并把同一字段同时写入 `ShadowCycleReport` / `RiskAssistCycleReport` 与 `shadow_cycle` / `risk_assist_cycle` audit payload，后续对账不必再手动 zip `command_types` 和 `command_reasons`。
- 继续补强 `runtime_overview`：把 reconciliation 实际使用的 `expected_position_state / expected_direction / expected_size_pct` 一并压进摘要，后续可以直接同屏比对 expected / runtime / observed 三层仓位口径，不用再倒推这轮 mismatch 是按哪套期望值算的。
- 同步更新 `tests/test_shadow_orchestrator.py`：给 shadow 与 risk-assist 的 simulated-real `runtime_overview` 断言补入 expected 字段，钉住对账基线本身也已经稳定落盘。
- 收口 `src/bot/exchange_adapter.py` 的 real-mode breakeven 语义：`advance_breakeven_stop` 不再统一硬拦截，而是在 live `ENTERED` 仓位、方向匹配、且 `entry_price` 可用时，直接把 stop 解析到保本位 `entry_price`，并以 `STOP_MARKET + closePosition=true` 下发真实请求。
- 为 breakeven 新增单独 real-mode 安全解析与审计痕迹：`prepared_request.body` 现在会稳定写入 `resolved_from_entry_price`、`resolved_stop_price` 与 `resolution_mode=breakeven_from_live_entry`，后续 review audit / execution result 时能直接看出这笔 stop 是按 live 保本位解析出来的。
- 保持 `advance_trailing_stop` 在 real mode 下继续硬拦截，不把量化侧 `trailing_rule / tp_ladder` 误翻译成 Binance `TRAILING_STOP_MARKET` 参数；这轮只放行“可由 live entry price 无歧义推导”的 breakeven，不猜 trailing 真实语义。
- 同步放宽 real mode runtime snapshot 预抓范围：除了 `maintain_protective_stop` 外，`advance_breakeven_stop` 现在也会提前抓取 `positionRisk/openOrders/account`，确保真实解析前能拿到 live `entry_price` 与方向，不靠 handoff 本地字段硬推。
- 补强 `tests/test_exchange_adapter.py`：新增 breakeven 在 `RuntimeMode.REAL` 下的成功映射断言，并覆盖无 live 持仓、方向不匹配、缺少 `breakeven_trigger` 三类拒绝场景，明确这些情况下都必须返回 `unsafe_request_mapping`。

### 本轮验证
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py -q` → `20 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py D:/eth_trading_bot/tests/test_exchange_adapter.py -q` → `45 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_exchange_adapter.py -q` → `28 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py -q` → `20 passed`

### 当前状态
- 机器人仓现在除了把 `recent fills / reconciliation` 摘要沉淀进 `state_store` 外，也已经把同一层摘要稳定写进 `shadow_cycle` 与 `risk_assist_cycle` audit 事件，后续排查可以直接看 `audit.jsonl`。
- `risk_assist_cycle` 现在不只覆盖“是否写摘要”，还对失败分类、失败后的本地恢复状态、reconciliation reason_codes 与 audit/state 组合优先级做了显式回归，后续更容易判断问题落在主执行、辅助对账还是保护单维护层。
- simulated-real 风险循环这条链当前也已确认：orchestrator → adapter → state_store → audit 能把成交摘要和 reconciliation 摘要完整传递下去。
- `breakeven / trailing stop` 在 real mode 下的硬边界现在有了更细粒度的回归保护，不再只依赖一条混合 stop 语义测试。
- 机器人主动作层现在已按“量化直出、机器人只做 guard + execution mapping”继续收口，`plan_reason` 在允许执行场景里也已统一切到执行层直通语义，不再保留 `entry_allowed / reduce_allowed / exit_allowed` 这类旧标签。
- 现在 primary command 的 `reason` 也已从泛化 `plan_reason` 里拆开，收口为 `effective_action:<action>`，后续 review audit / action / result 三层时更容易直接比对主执行语义。
- report 和 audit 展示层现在也已显式落下 `command_reasons`，后续 simulated-real 联调时能直接对照“量化请求动作、执行层最终动作、实际命令原因”三层，不必每次展开整包 command payload 才看得清。
- real mode 下的 `breakeven` 现在已从“统一阻断”收口成“仅在 live position + live entry price 可验证时才放行”的窄安全映射；`trailing stop` 仍继续保持硬拦截，当前真实执行边界更清晰。

### 下一步
- 继续看是否需要把 audit/report 里与 `requested_action / effective_action / command reason` 相关的字段再进一步标准化，减少后续联调时的口径切换成本。
- 如果后续开始做更接近联调的 simulated-real 回放，可直接复用本轮新增的 audit 与 state 摘要字段做结果比对，并继续验证量化直连执行层口径是否贯穿全链路。
- 后续若要继续推进实盘保护语义，优先补 `trailing stop` 的 Binance 真实参数合同，而不是在机器人侧猜 `trailing_rule / tp_ladder` 到 `TRAILING_STOP_MARKET` 的映射。

### 今日完成
- 继续沿昨天停点推进，没有再扩机器人范围，而是先把 `recent fills / reconciliation / simulated-real` 这条链的摘要语义补实。
- 调整 `state_store` 的 `recent_fill_summary` 写入口径：如果 `sync_recent_fills` 返回里已经带有 `response_summary`，优先收口成这层结构化摘要，而不是把整包 execution details 原样塞进状态里。
- 扩展 Binance `sync_recent_fills` 的响应摘要，除了 `fill_count / latest_trade_id / latest_order_id / latest_realized_pnl`，继续补了 `distinct_order_count`、`latest_side`、`latest_price`、`latest_qty`、`latest_quote_qty`、`latest_time`，方便后续 audit / reconciliation 判断最近成交是否真的更新。
- 扩展 `reconcile_position_and_orders` 的响应摘要，继续补入 `mark_price`、`notional`、`account_equity` 与 `account_equity_source`，让这轮 reconciliation 不只知道“当前 size_pct 是多少”，还知道 size_pct 依据的是哪一层账户权益字段。
- 把账户权益抽取逻辑拆成 `value + source` 两层返回，保留原有 `size_pct` 归一化口径不变，但让 audit / response_summary 能直接看到实际采用了 `totalWalletBalance` 还是 `assets.USDT.*`。
- 新增并跑通 simulated-real 端到端链路回归：确认 orchestrator 在 `SIMULATED_REAL` 模式下，会把 `sync_recent_fills` 与 `reconcile_position_and_orders` 的结构化摘要真正写进 `state_store`，而不是只在 command result 里短暂存在。

### 定向验证
- `python -m pytest D:/eth_trading_bot/tests/test_exchange_adapter.py D:/eth_trading_bot/tests/test_state_store.py D:/eth_trading_bot/tests/test_shadow_orchestrator.py -q` → `44 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_position_manager.py -q` → `6 passed`

### 当前状态
- 机器人仓现在已经不只是能做真实请求映射和基础对账，还能把 `recent fills` 与 `reconciliation` 的关键摘要稳定沉淀进本地状态，便于后续 audit 与恢复逻辑继续消费。
- `breakeven / trailing stop` 在 real mode 下仍然保持硬拦截，没有因为这轮补摘要而被误放行。
- `simulated-real` 这条链当前已确认：orchestrator → adapter → state_store 能把成交摘要和对账摘要完整传递下去。

### 下一步
- 继续把 `recent fills` 与 reconciliation 的更深层状态收口到 audit 语义，而不只是 state_store 摘要。
- 再补一轮针对 `breakeven / trailing stop` real-mode 拦截边界的联调级验证，确认后续不会把 strategy-relative 语义误送到 Binance。

### 今日完成
- 完成机器人项目首版框架搭建与文档落盘。
- 接通 Binance `signer / transport / real adapter` 最小真实执行链路。
- 修复 `strict-live` 4 轮汇总报告误判成功问题，并回刷当前报告。
- 完善 Binance 请求字段：补充 `side`、`newOrderRespType=RESULT`、`reduceOnly`、`limit=20` 等更贴近真实执行的参数。
- 为 `sync_recent_fills` 与 `reconcile_position_and_orders` 增加结构化响应摘要，便于后续对账和状态迁移。
- 接通真实持仓保护单的窄安全映射：仅在 live position 已存在、方向明确、`entry_price` 可用、且只携带 `initial_stop_loss` 时，才把相对止损倍率换算成 Binance 可执行的绝对 `stopPrice`。
- 保持 `breakeven / trailing / tp_ladder` 等仍属 strategy-relative 的保护语义在 real mode 下继续硬拦截，避免把未收口字段误发到 Binance。
- 收紧 real mode 的 runtime snapshot 预抓范围，仅在 `maintain_protective_stop` 时才提前抓取 `positionRisk/openOrders`，避免普通真实下单额外增加无关请求。
- 接通真实仓位数量到内部 `position_size_pct` 的首版归一化：在 Binance `positionRisk` 存在持仓时补抓 `account`，用 `abs(notional) / (account_equity * leverage)` 计算 live `size_pct`，让 runtime snapshot 不再长期停留在“有持仓但 size_pct=0”的失真状态。
- 为 runtime snapshot 增加账户权益缺失时的保守兜底：若 `account` 无法提供可用权益，则继续返回 `size_pct=0.0`，避免在口径未确认时伪造仓位比例。
- 把真实 `size_pct` 继续接进 reconciliation 链路：`reconcile_position_and_orders` 在 real mode 下如 live position 存在，会额外补抓 `account`，并把 `size_pct / leverage` 一并写入 `response_summary`。
- 调整 `state_store` 的 reconciliation 回写优先级：非 flat 场景先吃 live `response_summary.size_pct`，只有缺失时才退回 handoff，避免刚对账完又被本地旧 handoff 覆盖。
- 补充并跑通新的 adapter / state_store 回归，锁定 `positionRisk + account` 的 reconciliation 摘要路径，以及 live `size_pct` 回写本地状态的路径。

### 当前状态
- `D:\eth_trading_bot` 已具备 shadow / simulated-real / real 三种运行模式骨架。
- 实盘链路目前已具备：请求签名、代理透传、超时/HTTP/JSON 错误映射、基础响应摘要、真实 `size_pct` 归一化，以及 reconciliation 对 live size 的状态回写。
- 仍未完成：`recent fills` 与 reconciliation 的更深层状态收口、`breakeven / trailing stop` 的 Binance 真实参数语义、end-to-end `simulated-real` 联调。

### 明天计划
- 继续把 `userTrades / positionRisk` 真实返回接入更完整的 reconciliation / audit 语义。
- 继续收口 `breakeven / trailing stop` 的 Binance 真实参数语义，避免倍率字段误映射成真实价格。
- 跑一轮 end-to-end `simulated-real` 联调，确认 orchestrator → adapter → state_store → audit 全链路一致。
- 如联调稳定，再准备最小仓位 real 模式前的检查清单。

### 今日新增进展（real entry）
- 收口 `src/bot/exchange_adapter.py` 的 real-mode `entry_order` 数量合同：不再裸发 `MARKET` 单，而是在 real 校验层先确认 live 仓位必须为 `FLAT`，再基于量化侧已存在的 `position_size_pct`，结合 Binance runtime `account_equity`、live leverage、以及 `markPrice`，解析出可执行 `quantity`。
- 机器人侧没有新造第二层策略字段，也没有在旧仓补 quantity 常量；仍然坚持“量化给仓位比例，机器人只做 live 校验与交易所映射”的边界。真实数量解析公式当前收口为 `position_size_pct * account_equity * leverage / mark_price`。
- 为避免机器人本地拍脑袋写死步长，这轮改为实时读取 Binance `exchangeInfo` 的 `LOT_SIZE/MARKET_LOT_SIZE` 元数据，使用 `minQty` 与 `stepSize` 对 `quantity` 做真实收口；解析后低于交易所最小可下数量时，稳定返回 `unsafe_request_mapping`，不发真实请求。
- 同步扩展 `PositionSnapshot` 与 `EntryOrderPayload`：runtime snapshot 现在显式保留 `mark_price`，entry payload 现在显式保留量化 handoff 下发的 `position_size_pct`，便于后续 audit / preflight 直接看到这笔 quantity 是按哪套实时输入推出来的。
- 补强 `tests/test_exchange_adapter.py`：新增 real entry 在 `RuntimeMode.REAL` 下的成功、交易所拒绝、timeout、非空仓阻断、`position_size_pct<=0` 阻断、`quantity < minQty` 阻断等回归，锁定实盘前最关键的数量护栏。
- 继续收口一层“只校验不发单”的 preflight 能力：给 `RealExchangeAdapter` 新增 `preflight_commands(...)`，允许在 `RuntimeMode.REAL` 语义下提前拉 runtime/account/exchangeInfo/premiumIndex，验证 entry quantity 是否能被真实解析并签名，但不向 `/fapi/v1/order` 发送真实订单。
- preflight 的结果会显式返回 `preflight_ready` / `unsafe_request_mapping` / `request_signing_failed` / `transport_timeout` 等执行前状态，后续可以直接拿来做最小实盘前检查清单，而不用真的先打一笔订单试错。

### 本轮验证（real entry）
- `python -m pytest D:/eth_trading_bot/tests/test_exchange_adapter.py -q` → `32 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py -q` → `22 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_binance_transport.py -q` → `9 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_config.py -q` → `3 passed`

### 当前状态（real entry）
- 机器人仓当前已经不再缺少实盘 entry 的最小数量合同；只要量化 handoff 给出 `position_size_pct`，且 Binance runtime 能提供 live leverage / account equity / mark price / stepSize / minQty，机器人就能把 entry 安全收口成真实 `quantity`。
- `entry_order` 当前仍然保持窄边界：只允许空仓首开，不允许在 live `ENTERED` 状态下再发 entry；一旦 runtime/account/交易所元数据不完整，或解析数量低于 `minQty`，就直接拒绝。
- 现在已经具备“先 preflight，再决定是否尝试最小实盘单”的条件；下一步可以不碰真实下单，先用 `preflight_commands(...)` 把当前账户、标记价、交易所步长、最小数量是否都能顺利解析一遍。

### 下一步（real entry）
- 先基于 `preflight_commands(...)` 做一轮最小实盘前检查，确认当前 Binance runtime 下 entry quantity 能否稳定解析，不急着直接发真实订单。
- 如果 preflight 稳定，再决定是否进入真正的小仓首单尝试；若仍出现 transport / signing / minQty / account equity 问题，就继续在 preflight 层收口，不靠实盘试错。

- 不再让机器人继续硬猜 `trailing_rule / tp_ladder`，而是先把量化侧 `ExitPlan / ExecutionHandoff` 合同补成显式 trailing 执行字段：`trailing_activation_ratio` 与 `trailing_callback_rate_pct`，确保 trailing 真实参数仍由量化侧给出，机器人只做执行映射。
- 同步更新 `D:\quant_system_rebuild\src\interfaces\runner.py` 的 handoff 透传路径，让新增 trailing 字段可以从 decision engine 一路进入机器人执行层，不再只剩 explainability 用的 `trailing_rule` 标签。
- 收口 `D:\eth_trading_bot\src\bot\exchange_adapter.py` 的 real-mode trailing 语义：`advance_trailing_stop` 现在会在 live `ENTERED` 仓位、方向匹配、且 `entry_price` 可用时，基于量化下发的 `trailing_activation_ratio` 解析 `activationPrice`，并把请求类型切到 Binance `TRAILING_STOP_MARKET`。
- 机器人侧 trailing 真实下单现在只消费量化显式执行字段：`activationPrice` 来自 live `entry_price * trailing_activation_ratio`，`callbackRate` 来自 `trailing_callback_rate_pct`；若缺字段、方向不符、无 live 持仓、或解析后参数不合法，则继续稳定返回 `unsafe_request_mapping`。
- 给 trailing 单独补了一套 real-mode 安全解析：不复用 protective-stop 的倍率规则，而是改用独立的 trailing activation 校验口径，明确 long 必须 `ratio > 1`、short 必须 `0 < ratio < 1`，避免把 trailing 激活价误按止损价语义处理。
- 同步补强 `tests/test_exchange_adapter.py`：新增 trailing 在 `RuntimeMode.REAL` 下的成功映射断言，并覆盖缺 `trailing_activation_ratio`、缺 `trailing_callback_rate_pct` 等拒绝场景，确认真实请求只会在显式合同完整时放行。

### 本轮验证（trailing）
- `python -m pytest D:/quant_system_rebuild/tests/test_contracts.py D:/quant_system_rebuild/tests/test_data_flow.py D:/quant_system_rebuild/tests/test_interfaces_execution_tracking.py D:/quant_system_rebuild/tests/test_interfaces_observation.py D:/quant_system_rebuild/tests/test_interfaces_learning.py D:/quant_system_rebuild/tests/test_policy_engine.py -q` → `153 passed`
- `python -m pytest D:/quant_system_rebuild/tests/test_interfaces_runner.py D:/quant_system_rebuild/tests/test_interfaces_scheduler.py -q` → `59 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_exchange_adapter.py -q` → `30 passed`
- `python -m pytest D:/eth_trading_bot/tests/test_shadow_orchestrator.py -q` → `20 passed`

### 当前状态（更新）
- 机器人仓的 real-mode 保护语义现在已经从“只放 initial stop、breakeven/trailing 全拦”推进到“initial protective stop / breakeven / trailing 都有各自独立的窄安全映射”，且都要求 live 仓位与显式执行合同可验证，机器人本身不再补第二层策略判断。
- `advance_trailing_stop` 现在已经能在量化显式合同完整时，安全映射到 Binance `TRAILING_STOP_MARKET`；`trailing_rule / tp_ladder` 仍保留为策略解释语义，但不再被机器人直接拿来臆造交易所参数。
- 当前这条“量化直出执行语义 → 机器人 guard / mapping → Binance 请求”的主链路，`breakeven + trailing` 两块都已经完成并通过定向回归。

### 下一步（更新）
- 如果后续继续推进联调，可优先在 simulated-real / 小仓 real 里验证 `breakeven / trailing` 的 audit、execution result、runtime overview 三层展示是否足够清晰。
- 若量化侧后面还要继续细化 trailing 策略，只继续改量化产出的执行合同；机器人侧保持只做 live 校验和交易所参数映射，不再回退到根据策略标签猜参数的老路。
